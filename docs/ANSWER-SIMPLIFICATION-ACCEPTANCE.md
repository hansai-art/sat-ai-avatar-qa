# 答案精簡驗收（2026-09-23）

本次依學生閱讀需求改掉目錄式長文。沿用既有問題 ID、來源對照、分類與搜尋，未加入 AI API、通知後端或付費服務。

## 改動

- 62 篇正式答案逐題精簡，將必要的第一步、路徑與條件寫進正文，不再分成重複區塊。
- 移除桌機與手機「這一頁的內容」、捲動目錄程式，以及「先看結論／先做這一步／對照畫面」等制式標題。
- 保留短答底色、操作步驟、必要提醒、官方指南與回課程提問連結。提問入口是外連，沒有宣稱自動送出通知。
- 已有兩張真實截圖保留小預覽與放大檢視；未捏造缺少的介面截圖。
- 相關題目只列題名。匿名來源、原留言對照、編輯日期與核對資訊在底部收合。
- `#conclusion` 與 `#first-step` 可直接分享。手機版課程章節導覽仍為收合。

## 本機驗收

- 正式建置：62 題、110 HTML，來源映射及學員匿名驗證通過。
- 既有瀏覽器套件：80 passed，20 skipped（示範資料專用情境）。涵蓋搜尋、交集篩選、章節、分頁、返回位置、分享、圖片檢視與無 JavaScript 閱讀。
- 360、390、430px：無橫向溢出，BotFather 頁兩個操作步驟在 844px 高度的首屏內。
- 390×844 的 BotFather 頁，操作正文起點從 1129px 提前到約 497px。這是 Playwright 模擬手機寬度的量測，不是真實手機操作驗收。
- 內容稽核：所有問題的題名、來源、匿名標示、課程與分類對照、排序與相關題目資料保持不變。
- 原「簡答＋第一步＋正文」共 25,600 字元，新「簡答＋正文」15,912 字元，約減少 37.8%。此數字包含 Markdown 標記，不代表學生實際閱讀時間。

發布後還需核對 Cloudflare 的 `github:push` 部署、公開 `build-info.json` 提交碼，並執行公開站驗收。這份文件記錄本機結果；公開站結果以實際部署讀回與驗收紀錄為準。
